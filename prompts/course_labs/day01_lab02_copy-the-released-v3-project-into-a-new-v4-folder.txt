IMPORTANT CURRENT-STATE OVERRIDE FOR DAY 1 LAB 2:

The V3-to-V4 baseline copy has already been completed before this lab.

The current V4 project already exists at:
~/Developer/AgentGuard/01-Working/agentguard-v4

The frozen V3 source project is:
~/Developer/AgentGuard/01-Working/agentguard-v3

Do NOT repeat the manual's `cp -R` operation.
Do NOT recreate, delete, overwrite, or reinitialize the V4 project.
Do NOT copy V3's `.git`, `.venv`, `.env`, caches, or other environment-specific/private files into V4.
Do NOT replace the V4 `prompts/course_labs/` prompt set or `docs/lab_execution_index.md` with the V3 versions.
Do NOT undo completed Day 1 Lab 1 work.

Treat this Lab 2 as a verification-and-learning lab:
1. Inspect the existing V3 and V4 project state.
2. Verify that V4 inherited the required released V1-V3 source baseline.
3. Explain what should and should not be copied when creating a new version.
4. Identify any unexpected baseline difference, but stop and ask before changing anything.
5. Do not modify files merely to make V4 byte-for-byte identical to V3, because intentional V4 course-scaffolding and completed Lab 1 changes already exist.
6. Present a short numbered plan and stop for my approval before making any change.

Use read-only inspection commands first. If the inherited baseline is already correct, the only Lab 2 content change I expect is an appropriate append-only learning-log entry, unless you identify a real problem and I separately approve a fix.

Preserve the currently passing V1-V3 regression baseline and stay entirely inside the two AgentGuard project folders named above.



I am a beginner completing AgentGuard v4, Day 1, Lab 2: Copy The Released V3 Project Into A New V4 Folder.

Read `CLAUDE.md`, `docs/lab_execution_index.md`, and the current repository before changing anything. Stay inside `~/Developer/AgentGuard/01-Working/agentguard-v4`.

## Learning goal
how to preserve the read-only integration as the trusted discovery baseline

## Before any change
1. Explain the concept in plain English and define every new technical term.
2. Explain the input, processing, output, and security boundary for this lab.
3. Inspect the files below and tell me whether each already exists.
4. Present a short numbered plan and stop for my approval.

## Exact relevant paths
- `notes/learning_log.md`
- `README.md`
- `START_HERE.md`
- `VERSION.txt`
- `CLAUDE.md`
- `docs/roadmap.md`

If the lab requires a missing file, create it at the exact path above. Do not invent a different filename or folder. Do not edit unrelated files unless you explain why and receive approval.

## Implementation requirements
- Complete only the behavior named in this lab: **copy the released v3 project into a new v4 folder**.
- Compare the work with the matching reference file in the starter kit, but explain the code instead of blindly copying it.
- Keep all existing V1–V3 behavior working.
- Use only synthetic data and dedicated test accounts/repositories.
- Never reveal, print, paste into chat, or commit API keys, tokens, passwords, SSH material, browser data, or personal files.
- Preserve deterministic security authority. AI output may explain or propose only within the version’s documented boundary.
- Add or update a readable automated test whenever executable behavior changes.
- Keep the code simple enough to explain line by line.

## Verification commands
After implementation, run these one at a time and explain each result:
1. `cd ~/Developer/AgentGuard/01-Working`
2. `cp -R agentguard-v3 agentguard-v4`
3. `cd agentguard-v4`
4. `pwd`

## Passing criteria
- The named files exist in the exact paths, the command completes without an unhandled error, and git shows only the expected changes.
- You can explain the input, processing, output, safety boundary, and reason this lab exists.

## Finish the lab
1. Show the exact files changed.
2. Show the exact test or command output.
3. Explain what passed, what was not tested, and any live-service step I still need to run manually.
4. Give me a 3–5 sentence interview explanation connecting this lab to enterprise AI security.
5. Tell me exactly what screenshot or text evidence to save.
6. Do not commit, push, create a pull request, spend API money, or run a live external action unless this lab explicitly requires it and I separately approve it.
