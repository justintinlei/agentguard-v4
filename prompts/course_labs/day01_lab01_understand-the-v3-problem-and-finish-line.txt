I am a beginner completing AgentGuard v3, Day 1, Lab 1: Understand The V3 Problem And Finish Line.

Read `CLAUDE.md`, `docs/lab_execution_index.md`, and the current repository before changing anything. Stay inside `~/Developer/AgentGuard/01-Working/agentguard-v3`.

## Learning goal
why direct file access does not represent enterprise integrations and why a protocol boundary matters

## Before any change
1. Explain the concept in plain English and define every new technical term.
2. Explain the input, processing, output, and security boundary for this lab.
3. Inspect the files below and tell me whether each already exists.
4. Present a short numbered plan and stop for my approval.

## Exact relevant paths
- `notes/learning_log.md`
- `README.md`
- `START_HERE.md`
- `VERSION.txt`
- `CLAUDE.md`
- `docs/roadmap.md`

If the lab requires a missing file, create it at the exact path above. Do not invent a different filename or folder. Do not edit unrelated files unless you explain why and receive approval.

## Implementation requirements
- Complete only the behavior named in this lab: **understand the v3 problem and finish line**.
- Compare the work with the matching reference file in the starter kit, but explain the code instead of blindly copying it.
- Keep all existing V1–V2 behavior working.
- Use only synthetic data and dedicated test accounts/repositories.
- Never reveal, print, paste into chat, or commit API keys, tokens, passwords, SSH material, browser data, or personal files.
- Preserve deterministic security authority. AI output may explain or propose only within the version’s documented boundary.
- Add or update a readable automated test whenever executable behavior changes.
- Keep the code simple enough to explain line by line.

## Verification commands
After implementation, run these one at a time and explain each result:
1. `cd ~/Developer/AgentGuard/01-Working/agentguard-v3`
2. `source .venv/bin/activate`
3. `git status --short`

## Passing criteria
- The named files exist in the exact paths, the command completes without an unhandled error, and git shows only the expected changes.
- You can explain the input, processing, output, safety boundary, and reason this lab exists.

## Finish the lab
1. Show the exact files changed.
2. Show the exact test or command output.
3. Explain what passed, what was not tested, and any live-service step I still need to run manually.
4. Give me a 3–5 sentence interview explanation connecting this lab to enterprise AI security.
5. Tell me exactly what screenshot or text evidence to save.
6. Do not commit, push, create a pull request, spend API money, or run a live external action unless this lab explicitly requires it and I separately approve it.


AgentGuard V3 Day 1 Lab 1 — Current-State Override

The official lab prompt above was written for a generic fresh-start path. Apply the following facts and constraints to this lab.

My actual project path

Use only:

~/Developer/AgentGuard/01-Working/agentguard-v3

Do not use or create ~/Developer/agentguard-v3.

Verified starting state

The V3 project already exists and was created from the verified released V2 baseline before beginning the V3 manual.

Current verified state:

V1 is complete and frozen.
V2 is complete and frozen and must not be modified.
V3 branch is main.
V3 baseline commit is:
84f249e Start V3 from verified AgentGuard V2 baseline
git status --short is clean before this lab.
V3 has its own fresh .venv.
Python resolves to the V3 .venv.
No V2 .env was copied into V3.
The inherited V2 baseline has already been verified to pass:
52 tests
V2 evaluations 3/3
full regression/evaluation matrix
secret check
release gate
setup verifier

These facts describe the starting baseline. Do not rerun future-lab work merely to reproduce them unless this lab specifically requires it.

Lab 1 scope rules

This is Day 1 Lab 1 only.

The purpose is to understand and document:

why direct file access is not representative of an enterprise integration;
why a controlled protocol boundary matters;
what V3 intends to add over the verified V2 baseline;
the input, processing, output, trust boundary, and security rationale.

Do not implement future V3 functionality in this lab.

Specifically, do not create or implement:

an MCP server;
an MCP client;
discovery core functions;
connected-environment fixtures;
MCP adapters;
MCP SDK configuration;
new V3 evaluations;
new Streamlit V3 functionality;
path-traversal or symlink defenses;
future V3 tests.

Those belong to later labs.

Documentation accuracy rule

Some inherited files may still describe AgentGuard V2. It is acceptable to propose documentation changes that establish V3 identity, its intended direction, and its security invariants.

However:

Do not claim that MCP functionality has already been implemented or tested when it has not.
Distinguish clearly between the verified current baseline and planned V3 capabilities.
Do not replace inherited files wholesale with final starter-kit versions merely because those versions describe the eventual finished V3 product.
Use the starter kit only as a read-only comparison/reference.
Preserve V1/V2 behavior and architecture unless the manual explicitly authorizes a later change.

The core invariant is:

V3 may change where inventory comes from, but V1's deterministic scanner remains the sole authority for risk.

Working method

Start in Plan mode.

Before changing anything:

Inspect the current repository and the six Lab 1 files.
Explain what is already V2-oriented versus already V3-oriented.
Explain what, if anything, actually needs to change for Lab 1.
Show the exact files you propose to modify.
Explicitly state that no future-lab implementation will be added.
Stop and wait for my approval.

After I approve the plan, use Manual mode rather than broad autonomous changes.

Do not commit, push, reset Git, change branches, or modify V1 or V2.

Do not access or reveal .env, API keys, credentials, SSH material, browser data, password stores, or unrelated personal files.

At the end, show:

exact files changed;
git status --short;
git diff --name-only;
the relevant readable git diff;
what was verified;
what was intentionally not tested because it belongs to later labs;
the evidence I should save.

Do not consider the lab complete merely because the edits are finished.

